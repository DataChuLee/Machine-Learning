# 학습 --> 지도학습(강화학습습) / 비지도 학습(unsupervised learning) 
# 지도학습(Supervised learning)이란 정답이 있는 데이터 분석이다.
# Labeling(라벨링) 되어 있는 데이터를 분석하는 것이다. 
# 라벨링이 되어 있는 데이터를 이용해서 모형을 만든다. 그 후에 새로운 데이터를 만들어 이 데이터가 어떤 라벨링으로 들어갈 것인지 판단한다.
# 지도학습 -> 분류 / 비지도학습 -> 군집화 